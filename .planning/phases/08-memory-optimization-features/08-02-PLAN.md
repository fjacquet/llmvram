---
phase: 08-memory-optimization-features
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/utils/schemas.ts
  - src/store/uiStore.ts
  - src/store/urlSerializer.ts
  - src/store/urlSerializer.test.ts
  - src/components/inputs/GradientAccumulationInput.tsx
  - src/components/inputs/OptimizationToggles.tsx
  - src/components/inputs/EffectiveBatchDisplay.tsx
  - src/components/inputs/TrainingPanel.tsx
autonomous: true

must_haves:
  truths:
    - "User can set gradient accumulation steps (1-128) via slider with presets"
    - "User can toggle gradient checkpointing on/off and see explanation of memory vs compute trade-off"
    - "User can toggle Flash Attention on/off and see sequence-length-dependent benefit explanation"
    - "User can see effective batch size calculated as micro-batch x accumulation x GPUs"
    - "Optimization settings persist in URL hash for sharing"
  artifacts:
    - path: "src/components/inputs/GradientAccumulationInput.tsx"
      provides: "Gradient accumulation steps input with slider and presets"
      min_lines: 40
    - path: "src/components/inputs/OptimizationToggles.tsx"
      provides: "Gradient checkpointing and Flash Attention toggle switches"
      min_lines: 50
    - path: "src/components/inputs/EffectiveBatchDisplay.tsx"
      provides: "Calculated effective batch size display with formula"
      min_lines: 25
    - path: "src/store/uiStore.ts"
      provides: "Optimization state fields and setters"
      contains: "gradientAccumulationSteps"
    - path: "src/store/urlSerializer.ts"
      provides: "URL persistence for optimization settings"
      contains: "ga:"
  key_links:
    - from: "src/components/inputs/GradientAccumulationInput.tsx"
      to: "src/store/uiStore.ts"
      via: "useUIStore hook for gradientAccumulationSteps"
      pattern: "useUIStore"
    - from: "src/components/inputs/OptimizationToggles.tsx"
      to: "src/store/uiStore.ts"
      via: "useUIStore hook for checkpointing and flash attention state"
      pattern: "useUIStore"
    - from: "src/components/inputs/EffectiveBatchDisplay.tsx"
      to: "src/engines/optimizations.ts"
      via: "import calculateEffectiveBatchSize"
      pattern: "calculateEffectiveBatchSize"
    - from: "src/components/inputs/TrainingPanel.tsx"
      to: "src/components/inputs/GradientAccumulationInput.tsx"
      via: "component import and render"
      pattern: "GradientAccumulationInput"
    - from: "src/store/urlSerializer.ts"
      to: "src/store/uiStore.ts"
      via: "serialize/deserialize optimization fields"
      pattern: "ga:|gc:|fa:"
---

<objective>
State management, URL persistence, and UI components for memory optimizations

Add optimization state (gradient accumulation steps, gradient checkpointing, Flash Attention)
to the Zustand store, persist in URL hash, and create the three input components that let users
configure these optimizations in the training panel.

Purpose: Complete the user-facing side of Phase 8 requirements (OPTIM-01 through OPTIM-04).
Users need to set gradient accumulation steps, toggle checkpointing/Flash Attention, and see
the effective batch size — all within the training configuration panel.

Output: Three new UI components integrated into TrainingPanel, store state with URL persistence.
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-memory-optimization-features/08-RESEARCH.md
@.planning/phases/08-memory-optimization-features/08-01-SUMMARY.md
@src/store/uiStore.ts
@src/store/urlSerializer.ts
@src/store/urlSerializer.test.ts
@src/utils/schemas.ts
@src/components/inputs/TrainingPanel.tsx
@src/components/inputs/BatchSizeInput.tsx
@src/components/inputs/ModeToggle.tsx
@src/components/layout/InputPanel.tsx
@src/engines/optimizations.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add optimization state to store and URL serializer</name>
  <files>
    src/utils/schemas.ts
    src/store/uiStore.ts
    src/store/urlSerializer.ts
    src/store/urlSerializer.test.ts
  </files>
  <action>
    1. In src/utils/schemas.ts, extend TrainingInputSchema with optimization fields:
       - gradientAccumulationSteps: z.number().int().min(1).max(128).default(1)
       - gradientCheckpointing: z.boolean().default(false)
       - flashAttention: z.boolean().default(false)

    2. In src/store/uiStore.ts, add three new state fields and setters:
       State fields (in UIState interface, under "// Training mode" comment block):
       - gradientAccumulationSteps: number (default: 1)
       - gradientCheckpointing: boolean (default: false)
       - flashAttention: boolean (default: false)

       Actions (in UIState interface):
       - setGradientAccumulationSteps: (steps: number) => void
       - setGradientCheckpointing: (enabled: boolean) => void
       - setFlashAttention: (enabled: boolean) => void

       Implementation in create() call (follow exact pattern of existing setters):
       - gradientAccumulationSteps: 1,
       - gradientCheckpointing: false,
       - flashAttention: false,
       - setGradientAccumulationSteps: (steps) => set({ gradientAccumulationSteps: steps }),
       - setGradientCheckpointing: (enabled) => set({ gradientCheckpointing: enabled }),
       - setFlashAttention: (enabled) => set({ flashAttention: enabled }),

    3. In src/store/urlSerializer.ts:
       a. Add to URLStateSchema (in training params section, after `tmp`):
          - ga: z.number().optional()    // gradientAccumulationSteps
          - gc: z.boolean().optional()   // gradientCheckpointing
          - fa: z.boolean().optional()   // flashAttention

       b. Update serializeToURL function signature to accept the 3 new fields:
          - gradientAccumulationSteps: number
          - gradientCheckpointing: boolean
          - flashAttention: boolean

       c. In the training state serialization block (the `...(state.mode === 'training' ? {...} : {})` section),
          add the 3 new short keys:
          - ga: state.gradientAccumulationSteps,
          - gc: state.gradientCheckpointing,
          - fa: state.flashAttention,

       d. deserializeFromURL already returns URLState which auto-includes new fields from schema.
          No changes needed to deserializeFromURL.

    4. In src/store/urlSerializer.test.ts, add test cases:
       - Test that optimization fields serialize correctly when mode is training
       - Test that optimization fields are absent when mode is inference
       - Test round-trip: serialize -> deserialize preserves optimization values
       - Test defaults: ga=1, gc=false, fa=false when fields absent
  </action>
  <verify>
    npm run typecheck
    npx vitest run src/store/urlSerializer.test.ts
    npm run lint
  </verify>
  <done>
    - uiStore has gradientAccumulationSteps, gradientCheckpointing, flashAttention fields with setters
    - URL serializer persists optimization state with short keys ga/gc/fa
    - URL round-trip preserves all optimization values
    - TypeScript and lint pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Create optimization UI components and integrate into TrainingPanel</name>
  <files>
    src/components/inputs/GradientAccumulationInput.tsx
    src/components/inputs/OptimizationToggles.tsx
    src/components/inputs/EffectiveBatchDisplay.tsx
    src/components/inputs/TrainingPanel.tsx
  </files>
  <action>
    1. Create src/components/inputs/GradientAccumulationInput.tsx:
       Follow the BatchSizeInput.tsx pattern exactly (slider + preset buttons).
       - Import useUIStore from '@store/uiStore'
       - Read gradientAccumulationSteps and setGradientAccumulationSteps from store
       - Slider: min=1, max=128, step=1
       - Presets: [1, 2, 4, 8, 16, 32, 64]
       - Label: "Gradient Accumulation Steps"
       - Display current value
       - Use same Tailwind classes as BatchSizeInput for visual consistency
       - Add aria-label and aria-valuetext for accessibility

    2. Create src/components/inputs/OptimizationToggles.tsx:
       Follow the ModeToggle.tsx pattern (Headless UI Switch).
       - Import { Description, Field, Label, Switch } from '@headlessui/react'
       - Import useUIStore from '@store/uiStore'
       - Read gradientCheckpointing, setGradientCheckpointing, flashAttention, setFlashAttention
       - Also read sequenceLength from store (for Flash Attention benefit display)
       - Two toggle switches in a single component:

       a. Gradient Checkpointing toggle:
          - Label: "Gradient Checkpointing"
          - Description when enabled: "~60% activation memory reduction, +20-25% training time"
          - Description when disabled: "Recompute activations to save memory (recommended for large models)"

       b. Flash Attention toggle:
          - Label: "Flash Attention"
          - Description when enabled: Show sequence-length-dependent benefit:
            - sequenceLength < 2048: "~15% activation reduction at current sequence length"
            - sequenceLength < 8192: "~50% activation reduction at current sequence length"
            - sequenceLength >= 8192: "~70% activation reduction at current sequence length"
          - Description when disabled: "O(n^2) to O(n) attention memory, also 2-4x faster"
          - Append to description: " (benefit scales with sequence length)"

       Use same Switch styling as ModeToggle (same className pattern for the toggle).
       Wrap each toggle in a Field component. Separate with spacing (space-y-4).

    3. Create src/components/inputs/EffectiveBatchDisplay.tsx:
       - Import useUIStore from '@store/uiStore'
       - Import { calculateEffectiveBatchSize } from '@engines/optimizations'
       - Read batchSize, gradientAccumulationSteps, numGPUs from store
       - Calculate: effectiveBatch = calculateEffectiveBatchSize(batchSize, gradientAccumulationSteps, numGPUs)
       - Display as a read-only info box (not editable):
         ```
         Effective Batch Size: {effectiveBatch}
         = {batchSize} (micro-batch) x {gradientAccumulationSteps} (accumulation) x {numGPUs} (GPUs)
         ```
       - Use a bg-blue-50 dark:bg-blue-900/20 info box style with rounded-lg padding
       - Label "Effective Batch Size" in font-semibold
       - Formula breakdown in text-sm text-gray-600 dark:text-gray-400
       - Only show this component when gradientAccumulationSteps > 1 OR numGPUs > 1
         (when both are 1, effective = micro-batch, no need to show)

    4. Update src/components/inputs/TrainingPanel.tsx:
       - Import the three new components:
         import { GradientAccumulationInput } from '@components/inputs/GradientAccumulationInput'
         import { OptimizationToggles } from '@components/inputs/OptimizationToggles'
         import { EffectiveBatchDisplay } from '@components/inputs/EffectiveBatchDisplay'
       - Add a new section below the existing pickers (after PrecisionPicker):
         ```
         <hr className="border-gray-200 dark:border-gray-700" />
         <h3 className="text-lg font-semibold text-gray-900 dark:text-white">
           Memory Optimizations
         </h3>
         <GradientAccumulationInput />
         <EffectiveBatchDisplay />
         <OptimizationToggles />
         ```
       - Order matters: accumulation first (users set batch config), then effective batch display
         (shows result), then optimization toggles (memory optimizations).

    5. Verify the full component tree renders:
       npm run build (includes typecheck)
  </action>
  <verify>
    npm run build
    npm run lint
  </verify>
  <done>
    - GradientAccumulationInput renders with slider (1-128) and preset buttons
    - OptimizationToggles renders two switches with descriptive labels
    - EffectiveBatchDisplay shows calculated effective batch with formula breakdown
    - TrainingPanel includes all three components under "Memory Optimizations" section
    - TypeScript compiles, Biome lint passes, production build succeeds
  </done>
</task>

</tasks>

<verification>
npm run build
npx vitest run
npm run lint

Manual verification (when running dev server):
1. Switch to Fine-tuning mode
2. See "Memory Optimizations" section in Training Configuration
3. Adjust gradient accumulation steps slider (1-128)
4. See effective batch size update when accumulation > 1 or GPUs > 1
5. Toggle gradient checkpointing, see description change
6. Toggle Flash Attention, see sequence-dependent benefit text
7. Copy URL, paste in new tab - all optimization settings preserved
</verification>

<success_criteria>
- Gradient accumulation steps input works (1-128 range, preset buttons)
- Effective batch size displays correct calculation (batch x accum x GPUs)
- Gradient checkpointing toggle shows memory/compute trade-off info
- Flash Attention toggle shows sequence-dependent benefit info
- All settings persist in URL hash
- Production build succeeds with no errors
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/08-memory-optimization-features/08-02-SUMMARY.md`
</output>
