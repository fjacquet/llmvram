---
phase: 01-foundation-data
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/data/gpus.json
  - src/utils/gpus.test.ts
autonomous: true

must_haves:
  truths:
    - "GPU database includes 17+ GPUs with accurate VRAM, bandwidth, and FLOPS specs"
    - "Database includes NVIDIA datacenter GPUs (A100, H100, H200, B200)"
    - "Database includes NVIDIA consumer GPUs (RTX 3090, 4090, 5090)"
    - "Database includes AMD MI300X"
    - "Database includes Apple Silicon (M1-M4 Ultra) with unified memory notation"
    - "All GPU entries pass Zod schema validation"
  artifacts:
    - path: "src/data/gpus.json"
      provides: "Curated GPU database with 17+ entries"
      min_lines: 200
    - path: "src/utils/gpus.test.ts"
      provides: "Validation tests for GPU data"
      exports: []
  key_links:
    - from: "src/data/gpus.json"
      to: "src/utils/schemas.ts"
      via: "GPUSchema validation"
      pattern: "validateGPUs"
---

<objective>
Create curated GPU database with 17+ GPUs covering NVIDIA datacenter (including H100, H200, B200, A100), NVIDIA consumer, AMD MI300X, and Apple Silicon M1-M4 Ultra with accurate VRAM, memory bandwidth, and FLOPS specifications sourced from official datasheets and dbgpu.

Purpose: Provide convenient presets for common GPU hardware while ensuring accuracy for VRAM calculations. Database must include both datacenter and consumer GPUs used by the team.

Output: Validated GPU database (src/data/gpus.json) with comprehensive test coverage.
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-data/01-RESEARCH.md
@.planning/phases/01-foundation-data/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Create GPU database with 15+ validated entries</name>
  <files>
src/data/gpus.json
  </files>
  <action>
Create src/data/gpus.json with 15+ GPUs covering datacenter, consumer, AMD, and Apple Silicon categories. Source specs from official datasheets and research sources (dbgpu, gpu-info-api, vendor specs).

**Required GPU entries (17+ total):**

**NVIDIA Datacenter (6 GPUs):**
1. H100 80GB PCIe - vram_gb: 80, memory_bandwidth_gbps: 2000, memory_type: "HBM3", bus_width: 5120, fp16_tflops: 989, fp32_tflops: 51, tdp_watts: 350, interconnect: "nvlink-4", tier: "datacenter"
2. H100 80GB SXM - vram_gb: 80, memory_bandwidth_gbps: 3350, memory_type: "HBM3", bus_width: 5120, fp16_tflops: 989, fp32_tflops: 51, tdp_watts: 700, interconnect: "nvlink-4", tier: "datacenter"
3. H200 141GB - vram_gb: 141, memory_bandwidth_gbps: 4800, memory_type: "HBM3e", bus_width: 5120, fp16_tflops: 989, fp32_tflops: 51, tdp_watts: 700, interconnect: "nvlink-4", tier: "datacenter"
4. B200 192GB - vram_gb: 192, memory_bandwidth_gbps: 8000, memory_type: "HBM3e", bus_width: 8192, fp16_tflops: 4500, fp32_tflops: 90, tdp_watts: 1000, interconnect: "nvlink-5", tier: "datacenter"
5. A100 80GB PCIe - vram_gb: 80, memory_bandwidth_gbps: 1935, memory_type: "HBM2e", bus_width: 5120, fp16_tflops: 312, fp32_tflops: 19.5, tdp_watts: 300, interconnect: "none", tier: "datacenter"
6. A100 80GB SXM - vram_gb: 80, memory_bandwidth_gbps: 2039, memory_type: "HBM2e", bus_width: 5120, fp16_tflops: 312, fp32_tflops: 19.5, tdp_watts: 400, interconnect: "nvlink", tier: "datacenter"

**NVIDIA Consumer (3 GPUs):**
5. RTX 5090 - vram_gb: 32, memory_bandwidth_gbps: 1792, memory_type: "GDDR7", bus_width: 512, fp16_tflops: 318, fp32_tflops: 159, tdp_watts: 575, tier: "consumer"
6. RTX 4090 - vram_gb: 24, memory_bandwidth_gbps: 1008, memory_type: "GDDR6X", bus_width: 384, fp16_tflops: 165, fp32_tflops: 82.6, tdp_watts: 450, tier: "consumer"
7. RTX 3090 - vram_gb: 24, memory_bandwidth_gbps: 936, memory_type: "GDDR6X", bus_width: 384, fp16_tflops: 71, fp32_tflops: 35.6, tdp_watts: 350, tier: "consumer"

**AMD Datacenter (1 GPU):**
10. MI300X - vram_gb: 192, memory_bandwidth_gbps: 5300, memory_type: "HBM3", bus_width: 8192, fp16_tflops: 1307, fp32_tflops: 163, tdp_watts: 750, interconnect: "infinity-fabric", tier: "datacenter"

**Apple Silicon (7 GPUs):**
11. M1 Ultra - vram_gb: 128, memory_bandwidth_gbps: 800, memory_type: "Unified", bus_width: 0, fp16_tflops: 21, tier: "apple-silicon", interconnect: "unified"
12. M2 Ultra - vram_gb: 192, memory_bandwidth_gbps: 800, memory_type: "Unified", bus_width: 0, fp16_tflops: 27, tier: "apple-silicon", interconnect: "unified"
13. M3 Ultra (estimated) - vram_gb: 192, memory_bandwidth_gbps: 800, memory_type: "Unified", bus_width: 0, fp16_tflops: 40, tier: "apple-silicon", interconnect: "unified"
14. M4 Max - vram_gb: 128, memory_bandwidth_gbps: 546, memory_type: "Unified", bus_width: 0, fp16_tflops: 29, tier: "apple-silicon", interconnect: "unified"
15. M3 Max - vram_gb: 128, memory_bandwidth_gbps: 400, memory_type: "Unified", bus_width: 0, fp16_tflops: 29, tier: "apple-silicon", interconnect: "unified"
16. M2 Max - vram_gb: 96, memory_bandwidth_gbps: 400, memory_type: "Unified", bus_width: 0, fp16_tflops: 13.6, tier: "apple-silicon", interconnect: "unified"
17. M1 Max - vram_gb: 64, memory_bandwidth_gbps: 400, memory_type: "Unified", bus_width: 0, fp16_tflops: 10.4, tier: "apple-silicon", interconnect: "unified"

**Format (JSON array):**
```json
[
  {
    "id": "nvidia-h100-80gb-pcie",
    "name": "NVIDIA H100 80GB PCIe",
    "manufacturer": "nvidia",
    "vram_gb": 80,
    "memory_bandwidth_gbps": 2000,
    "memory_type": "HBM3",
    "bus_width": 5120,
    "fp16_tflops": 989,
    "fp32_tflops": 51,
    "tdp_watts": 350,
    "interconnect": "nvlink-4",
    "tier": "datacenter"
  },
  ...
]
```

**Field mapping notes:**
- id: Lowercase with hyphens (e.g., "nvidia-h100-80gb-pcie")
- manufacturer: One of "nvidia", "amd", "apple", "intel" (lowercase)
- vram_gb: Total VRAM in GB (for Apple Silicon: unified memory capacity)
- memory_bandwidth_gbps: Memory bandwidth in GB/s (not Gbps)
- memory_type: "HBM3", "HBM2e", "GDDR6X", "GDDR7", "Unified" for Apple
- bus_width: Memory bus width in bits (0 for Apple Silicon unified memory)
- fp16_tflops: FP16 TFLOPS (optional but recommended)
- fp32_tflops: FP32 TFLOPS (optional but recommended)
- tdp_watts: Thermal design power in watts (optional)
- interconnect: "none", "nvlink", "nvlink-4", "infinity-fabric", "unified" (optional)
- tier: "datacenter", "consumer", "apple-silicon"

**Sources:**
- NVIDIA H100: https://resources.nvidia.com/en-us-tensor-core/nvidia-tensor-core-gpu-datasheet
- NVIDIA A100: Official NVIDIA datasheet
- RTX 5090/4090/3090: Official NVIDIA specs and TechPowerUp GPU Database
- AMD MI300X: Official AMD datasheet (https://www.amd.com/content/dam/amd/en/documents/instinct-tech-docs/data-sheets/amd-instinct-mi300x-data-sheet.pdf)
- Apple Silicon: Wikipedia and Mac Observer M-series guide

Ensure all entries pass GPUSchema validation.
  </action>
  <verify>
npm run typecheck (should pass)
npm run lint (should pass)
Create a temporary test script to validate:
```bash
node -e "const {validateGPUs} = require('./src/utils/schemas.ts'); const gpus = require('./src/data/gpus.json'); console.log('Valid:', validateGPUs(gpus).length, 'GPUs')"
```
  </verify>
  <done>
- src/data/gpus.json exists with 17+ GPU entries
- All entries include required fields: id, name, manufacturer, vram_gb, memory_bandwidth_gbps, memory_type, bus_width, tier
- Database includes NVIDIA datacenter (H100 80GB PCIe/SXM, H200 141GB, B200 192GB, A100 80GB PCIe/SXM)
- Database includes NVIDIA consumer (RTX 5090, 4090, 3090)
- Database includes AMD MI300X
- Database includes Apple Silicon M1-M4 Max/Ultra
- All entries pass GPUSchema.parse() validation without errors
  </done>
</task>

<task type="auto">
  <name>Write validation tests for GPU data</name>
  <files>
src/utils/gpus.test.ts
  </files>
  <action>
Create comprehensive Vitest tests for GPU data validation:

**src/utils/gpus.test.ts:**
```typescript
import { describe, it, expect } from 'vitest'
import { GPUSchema, validateGPUs } from './schemas'
import gpusData from '@data/gpus.json'

describe('GPU Database Validation', () => {
  it('should have at least 17 GPUs', () => {
    expect(gpusData.length).toBeGreaterThanOrEqual(17)
  })

  it('should validate all GPU entries against schema', () => {
    const result = validateGPUs(gpusData)
    expect(result.length).toBe(gpusData.length)
  })

  it('should include NVIDIA datacenter GPUs (H100, H200, B200, A100)', () => {
    const nvidiaDC = gpusData.filter(
      (gpu) => gpu.manufacturer === 'nvidia' && gpu.tier === 'datacenter'
    )
    expect(nvidiaDC.length).toBeGreaterThanOrEqual(6)

    const h100 = gpusData.find((gpu) => gpu.id.includes('h100'))
    const h200 = gpusData.find((gpu) => gpu.id.includes('h200'))
    const b200 = gpusData.find((gpu) => gpu.id.includes('b200'))
    const a100 = gpusData.find((gpu) => gpu.id.includes('a100'))
    expect(h100).toBeDefined()
    expect(h200).toBeDefined()
    expect(b200).toBeDefined()
    expect(a100).toBeDefined()
  })

  it('should include NVIDIA consumer GPUs (RTX series)', () => {
    const nvidiaConsumer = gpusData.filter(
      (gpu) => gpu.manufacturer === 'nvidia' && gpu.tier === 'consumer'
    )
    expect(nvidiaConsumer.length).toBeGreaterThanOrEqual(3)

    const rtx5090 = gpusData.find((gpu) => gpu.id.includes('5090'))
    const rtx4090 = gpusData.find((gpu) => gpu.id.includes('4090'))
    const rtx3090 = gpusData.find((gpu) => gpu.id.includes('3090'))
    expect(rtx5090).toBeDefined()
    expect(rtx4090).toBeDefined()
    expect(rtx3090).toBeDefined()
  })

  it('should include AMD MI300X', () => {
    const mi300x = gpusData.find((gpu) => gpu.id.includes('mi300x'))
    expect(mi300x).toBeDefined()
    expect(mi300x?.manufacturer).toBe('amd')
    expect(mi300x?.vram_gb).toBe(192)
  })

  it('should include Apple Silicon GPUs', () => {
    const appleSilicon = gpusData.filter((gpu) => gpu.manufacturer === 'apple')
    expect(appleSilicon.length).toBeGreaterThanOrEqual(4)

    const m1Ultra = gpusData.find((gpu) => gpu.id.includes('m1-ultra'))
    const m4Max = gpusData.find((gpu) => gpu.id.includes('m4-max'))
    expect(m1Ultra).toBeDefined()
    expect(m4Max).toBeDefined()
  })

  it('should have valid VRAM specifications', () => {
    gpusData.forEach((gpu) => {
      expect(gpu.vram_gb).toBeGreaterThan(0)
      expect(gpu.memory_bandwidth_gbps).toBeGreaterThan(0)
    })
  })

  it('should have consistent interconnect types', () => {
    const validInterconnects = ['none', 'nvlink', 'nvlink-4', 'infinity-fabric', 'unified', undefined]
    gpusData.forEach((gpu) => {
      expect(validInterconnects).toContain(gpu.interconnect)
    })
  })

  it('should validate individual GPU entry structure', () => {
    const sampleGPU = gpusData[0]
    const result = GPUSchema.safeParse(sampleGPU)

    expect(result.success).toBe(true)
    if (result.success) {
      expect(result.data).toHaveProperty('id')
      expect(result.data).toHaveProperty('name')
      expect(result.data).toHaveProperty('vram_gb')
      expect(result.data).toHaveProperty('memory_bandwidth_gbps')
    }
  })

  it('should have unique GPU IDs', () => {
    const ids = gpusData.map((gpu) => gpu.id)
    const uniqueIds = new Set(ids)
    expect(uniqueIds.size).toBe(ids.length)
  })

  it('should have performance specs for most GPUs', () => {
    const withFP16 = gpusData.filter((gpu) => gpu.fp16_tflops !== undefined)
    // At least 80% should have FP16 specs
    expect(withFP16.length).toBeGreaterThanOrEqual(Math.floor(gpusData.length * 0.8))
  })
})
```

This test suite ensures:
1. Minimum 17 GPUs in database (DATA-05)
2. All entries pass Zod validation
3. Required GPU categories are present (NVIDIA DC including H100/H200/B200/A100, consumer, AMD, Apple)
4. Specs are valid and complete
5. No duplicate IDs
6. Performance metrics are included
  </action>
  <verify>
npm run test (should pass all GPU validation tests)
npm run test:coverage (should show coverage for schema validation)
  </verify>
  <done>
- src/utils/gpus.test.ts exists with comprehensive test coverage
- All tests pass when running npm run test
- Tests verify minimum 17 GPUs in database
- Tests verify all required GPU categories are present (including H100, H200, B200, A100)
- Tests verify all entries pass Zod schema validation
- Tests verify unique IDs and valid specs
  </done>
</task>

</tasks>

<verification>
1. Run `npm run test` - all GPU validation tests should pass
2. Verify src/data/gpus.json contains 17+ entries
3. Verify database includes all required GPU categories (NVIDIA DC including H100/H200/B200/A100, consumer, AMD, Apple)
4. Run `npm run typecheck` and `npm run lint` - should pass with zero errors
5. Manually inspect a few GPU entries to verify accuracy of specs
</verification>

<success_criteria>
- GPU database includes 17+ GPUs with accurate VRAM, bandwidth, and FLOPS specs
- Database includes NVIDIA datacenter (A100, H100, H200, B200)
- Database includes NVIDIA consumer (RTX 3090, 4090, 5090)
- Database includes AMD MI300X
- Database includes Apple Silicon (M1-M4 Ultra) with unified memory notation
- All GPU entries pass Zod schema validation
- Comprehensive test coverage for data validation
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-data/01-02-SUMMARY.md`
</output>
