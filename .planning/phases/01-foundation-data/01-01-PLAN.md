---
phase: 01-foundation-data
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.app.json
  - tsconfig.node.json
  - vite.config.ts
  - vitest.config.ts
  - biome.json
  - tailwind.config.ts
  - index.html
  - src/main.tsx
  - src/App.tsx
  - src/test/setup.ts
  - src/types/gpu.ts
  - src/types/model.ts
  - src/utils/schemas.ts
autonomous: true

must_haves:
  truths:
    - "Developer can run npm install without errors"
    - "TypeScript compiles with strict mode enabled"
    - "Biome linting runs with zero errors"
    - "Path aliases resolve correctly in imports"
    - "Zod schemas validate GPU and Model data structures"
  artifacts:
    - path: "package.json"
      provides: "Project dependencies matching raidy versions"
      contains: "react.*19.2.0"
    - path: "tsconfig.app.json"
      provides: "TypeScript strict mode and path aliases"
      contains: "strict.*true"
    - path: "vite.config.ts"
      provides: "Vite build config with Tailwind plugin"
      exports: ["default"]
    - path: "vitest.config.ts"
      provides: "Vitest test configuration"
      exports: ["default"]
    - path: "biome.json"
      provides: "Biome linting rules matching raidy"
      contains: "recommended.*true"
    - path: "src/utils/schemas.ts"
      provides: "Zod schemas for GPU and Model validation"
      exports: ["GPUSchema", "ModelSchema"]
    - path: "src/types/gpu.ts"
      provides: "GPU TypeScript interface"
      exports: ["GPU"]
    - path: "src/types/model.ts"
      provides: "Model TypeScript interface"
      exports: ["Model"]
  key_links:
    - from: "tsconfig.app.json"
      to: "vite.config.ts"
      via: "path aliases must match"
      pattern: "@engines|@components|@store|@types|@utils|@data|@hooks"
    - from: "src/types/gpu.ts"
      to: "src/utils/schemas.ts"
      via: "type inference from Zod"
      pattern: "z\\.infer<typeof GPUSchema>"
    - from: "src/types/model.ts"
      to: "src/utils/schemas.ts"
      via: "type inference from Zod"
      pattern: "z\\.infer<typeof ModelSchema>"
---

<objective>
Set up complete project infrastructure mirroring raidy's architecture with React 19, TypeScript strict mode, Vite, Tailwind CSS v4, Biome, Vitest, and Decimal.js. Define Zod schemas and TypeScript types for GPU and Model data structures.

Purpose: Establish a solid foundation with proven tooling and strict type safety critical for accurate VRAM calculations. Mirroring raidy ensures consistency across internal tools and leverages known-good configurations.

Output: Fully configured project scaffold with passing linting, type checking, and test setup ready for database creation.
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-data/01-RESEARCH.md
@/Users/fjacquet/Projects/raidy/package.json
@/Users/fjacquet/Projects/raidy/biome.json
@/Users/fjacquet/Projects/raidy/tsconfig.app.json
@/Users/fjacquet/Projects/raidy/vite.config.ts
@/Users/fjacquet/Projects/raidy/vitest.config.ts
</context>

<tasks>

<task type="auto">
  <name>Set up project scaffold with exact raidy configuration</name>
  <files>
package.json
tsconfig.app.json
tsconfig.node.json
vite.config.ts
vitest.config.ts
biome.json
tailwind.config.ts
index.html
src/main.tsx
src/App.tsx
src/test/setup.ts
.gitignore
  </files>
  <action>
Create complete project infrastructure mirroring raidy's exact configuration:

**1. package.json:**
- name: "llmvram", version: "1.0.0", type: "module"
- Scripts: dev, build, preview, typecheck, lint, lint:fix, format, test, test:ui, test:coverage (match raidy)
- Dependencies: react@^19.2.0, react-dom@^19.2.0, zustand@^5.0.9, recharts@^3.6.0, zod@^3.25.76, decimal.js (latest), sonner@^2.0.7
- DevDependencies: typescript@~5.9.3, vite@^7.2.4, @vitejs/plugin-react@^5.1.1, @tailwindcss/vite@^4.1.18, tailwindcss@^4.1.18, @biomejs/biome@2.3.11, vitest@^4.0.16, jsdom@^27.4.0, @testing-library/react@^16.3.1, @testing-library/jest-dom@^6.9.1, @vitest/coverage-v8@^4.0.16, @vitest/ui@^4.0.16, @types/react@^19.2.5, @types/react-dom@^19.2.3, @types/node@^24.10.1

**2. tsconfig.app.json (strict mode with path aliases):**
- Copy raidy's tsconfig.app.json exactly
- Ensure strict: true, noImplicitAny: true, strictNullChecks: true, noUncheckedIndexedAccess: true
- Path aliases: @/, @engines/, @components/, @store/, @types/, @utils/, @data/, @hooks/
- target: ES2022, lib: ["ES2022", "DOM", "DOM.Iterable", "WebWorker"]

**3. tsconfig.node.json:**
```json
{
  "compilerOptions": {
    "composite": true,
    "skipLibCheck": true,
    "module": "ESNext",
    "moduleResolution": "bundler",
    "allowSyntheticDefaultImports": true,
    "strict": true
  },
  "include": ["vite.config.ts", "vitest.config.ts"]
}
```

**4. vite.config.ts:**
- Copy raidy's vite.config.ts with path alias resolution
- Change base to '/' (not '/raidy/' like raidy uses for GitHub Pages)
- Import tailwindcss from '@tailwindcss/vite'
- plugins: [react(), tailwindcss()]
- resolve.alias must match tsconfig.app.json paths exactly

**5. vitest.config.ts:**
- Copy raidy's vitest.config.ts
- globals: true, environment: 'jsdom'
- setupFiles: ['./src/test/setup.ts']
- include: ['src/**/*.{test,spec}.{ts,tsx}', 'tests/**/*.{test,spec}.{ts,tsx}']
- coverage.include: ['src/engines/**/*.ts', 'src/utils/**/*.ts']
- coverage.thresholds: 75% for lines/functions/branches/statements

**6. biome.json:**
- Copy raidy's biome.json exactly
- Schema: "https://biomejs.dev/schemas/2.3.11/schema.json"
- formatter: indentWidth: 2, lineWidth: 100
- javascript.formatter: quoteStyle: 'single', semicolons: 'asNeeded'
- linter.rules.recommended: true, correctness.noUnusedImports: 'error'

**7. tailwind.config.ts:**
```typescript
import type { Config } from 'tailwindcss'

export default {
  content: ['./index.html', './src/**/*.{js,ts,jsx,tsx}'],
} satisfies Config
```

**8. index.html:**
```html
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>LLM VRAM Calculator</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

**9. src/main.tsx:**
```typescript
import { StrictMode } from 'react'
import { createRoot } from 'react-dom/client'
import App from './App'

const root = document.getElementById('root')
if (!root) throw new Error('Root element not found')

createRoot(root).render(
  <StrictMode>
    <App />
  </StrictMode>
)
```

**10. src/App.tsx:**
```typescript
export default function App() {
  return (
    <div className="min-h-screen bg-gray-50 p-8">
      <h1 className="text-3xl font-bold text-gray-900">LLM VRAM Calculator</h1>
      <p className="mt-2 text-gray-600">Foundation ready - Phase 1</p>
    </div>
  )
}
```

**11. src/test/setup.ts:**
```typescript
import '@testing-library/jest-dom/vitest'
import { afterEach } from 'vitest'
import { cleanup } from '@testing-library/react'

afterEach(() => {
  cleanup()
})
```

**12. .gitignore:**
```
# Dependencies
node_modules/

# Build outputs
dist/
dist-ssr/
*.local

# Environment
.env
.env.local
.env.*.local

# Editor
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Test coverage
coverage/

# TypeScript
*.tsbuildinfo
```

Create directory structure: src/engines/, src/data/, src/types/, src/store/, src/components/, src/utils/, src/hooks/, src/test/
  </action>
  <verify>
npm install (should complete without errors)
npm run typecheck (should pass with no errors)
npm run lint (should pass with zero errors)
npm run dev (should start dev server on http://localhost:5173)
  </verify>
  <done>
- package.json exists with exact raidy versions for all dependencies
- TypeScript strict mode enabled in tsconfig.app.json
- Path aliases configured identically in tsconfig.app.json, vite.config.ts, and vitest.config.ts
- Biome linting passes with zero errors
- Dev server starts and shows "LLM VRAM Calculator" heading
- All required src/ directories created
  </done>
</task>

<task type="auto">
  <name>Define Zod schemas and TypeScript types for GPU and Model</name>
  <files>
src/types/gpu.ts
src/types/model.ts
src/utils/schemas.ts
  </files>
  <action>
Create Zod schemas and derive TypeScript types following the research patterns:

**1. src/utils/schemas.ts:**
```typescript
import { z } from 'zod'

// GPU Schema based on research (dbgpu fields)
export const GPUSchema = z.object({
  id: z.string().min(1),
  name: z.string(),
  manufacturer: z.enum(['nvidia', 'amd', 'apple', 'intel']),
  vram_gb: z.number().positive(),
  memory_bandwidth_gbps: z.number().positive(),
  memory_type: z.string(),
  bus_width: z.number().int().positive(),

  // Performance (optional for inference speed estimation)
  fp16_tflops: z.number().positive().optional(),
  fp32_tflops: z.number().positive().optional(),

  // Power and interconnect
  tdp_watts: z.number().positive().optional(),
  interconnect: z.enum(['none', 'nvlink', 'nvlink-4', 'infinity-fabric', 'unified']).optional(),

  // Classification
  tier: z.enum(['datacenter', 'consumer', 'apple-silicon']),
})

// Model Schema based on HuggingFace config.json fields
export const ModelSchema = z.object({
  id: z.string().min(1),
  name: z.string(),
  architecture: z.enum(['dense', 'moe']),
  num_parameters_billion: z.number().positive(),
  hidden_size: z.number().int().positive(),
  num_hidden_layers: z.number().int().positive(),
  num_attention_heads: z.number().int().positive(),

  // GQA field (optional - fallback to num_attention_heads if missing)
  num_kv_heads: z.number().int().positive().optional(),

  intermediate_size: z.number().int().positive(),

  // MoE fields (optional, only for MoE architectures)
  num_experts: z.number().int().positive().optional(),
  num_experts_per_token: z.number().int().positive().optional(),
})

// Export inferred types
export type GPU = z.infer<typeof GPUSchema>
export type Model = z.infer<typeof ModelSchema>

// Validation helper functions
export function validateGPU(data: unknown): GPU {
  return GPUSchema.parse(data)
}

export function validateModel(data: unknown): Model {
  return ModelSchema.parse(data)
}

export function validateGPUs(data: unknown): GPU[] {
  return z.array(GPUSchema).parse(data)
}

export function validateModels(data: unknown): Model[] {
  return z.array(ModelSchema).parse(data)
}
```

**2. src/types/gpu.ts:**
```typescript
import type { GPU as GPUBase } from '@utils/schemas'

// Re-export the Zod-inferred type
export type GPU = GPUBase

// Type for custom GPU input (all fields optional except vram_gb)
export interface CustomGPUInput {
  name: string
  vram_gb: number
  memory_bandwidth_gbps?: number
  fp16_tflops?: number
  fp32_tflops?: number
}

// Helper to convert custom input to GPU
export function createCustomGPU(input: CustomGPUInput): GPU {
  return {
    id: `custom-${Date.now()}`,
    name: input.name,
    manufacturer: 'nvidia', // Default for custom
    vram_gb: input.vram_gb,
    memory_bandwidth_gbps: input.memory_bandwidth_gbps || 0,
    memory_type: 'Custom',
    bus_width: 0,
    fp16_tflops: input.fp16_tflops,
    fp32_tflops: input.fp32_tflops,
    tier: 'consumer',
    interconnect: 'none',
  }
}
```

**3. src/types/model.ts:**
```typescript
import type { Model as ModelBase } from '@utils/schemas'

// Re-export the Zod-inferred type
export type Model = ModelBase

// Type for custom model input (minimal fields required)
export interface CustomModelInput {
  name: string
  num_parameters_billion: number
  hidden_size?: number
  num_hidden_layers?: number
  num_attention_heads?: number
  num_kv_heads?: number
  intermediate_size?: number
}

// Helper to create custom model with sensible defaults
export function createCustomModel(input: CustomModelInput): Model {
  // Default architecture ratios based on common LLM patterns
  const hiddenSize = input.hidden_size || 4096
  const numLayers = input.num_hidden_layers || 32
  const numHeads = input.num_attention_heads || 32

  return {
    id: `custom-${Date.now()}`,
    name: input.name,
    architecture: 'dense',
    num_parameters_billion: input.num_parameters_billion,
    hidden_size: hiddenSize,
    num_hidden_layers: numLayers,
    num_attention_heads: numHeads,
    num_kv_heads: input.num_kv_heads || Math.max(1, Math.floor(numHeads / 4)), // GQA default
    intermediate_size: input.intermediate_size || hiddenSize * 4, // Typical FFN expansion
  }
}
```

These schemas follow the exact field mappings from research and enable both curated database validation and custom user inputs per DATA-03 and DATA-04 requirements.
  </action>
  <verify>
npm run typecheck (should pass - types correctly inferred from Zod)
npm run lint (should pass - no unused imports or variables)
  </verify>
  <done>
- src/utils/schemas.ts exports GPUSchema, ModelSchema, GPU, Model types
- Zod schemas validate all required fields per research
- TypeScript types are inferred from Zod schemas using z.infer<>
- Custom input helpers exist in src/types/gpu.ts and src/types/model.ts
- No TypeScript or linting errors
  </done>
</task>

</tasks>

<verification>
1. Run `npm install && npm run dev` - should start dev server showing basic React app
2. Run `npm run typecheck` - should compile with zero TypeScript errors
3. Run `npm run lint` - should pass Biome linting with zero errors
4. Check that all path aliases resolve correctly in imports
5. Verify Zod schemas export correct types and validation functions
</verification>

<success_criteria>
- Developer can run `npm install && npm run dev` and see a basic React app with TypeScript strict mode
- Biome linting runs with zero errors on all code
- All configuration files match raidy patterns exactly
- Zod schemas define GPU and Model structures ready for database creation
- Path aliases work correctly across TypeScript, Vite, and Vitest configurations
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-data/01-01-SUMMARY.md`
</output>
