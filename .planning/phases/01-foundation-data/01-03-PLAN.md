---
phase: 01-foundation-data
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/data/models.json
  - src/utils/models.test.ts
autonomous: true

must_haves:
  truths:
    - "Model database includes 30+ popular models with accurate parameter counts"
    - "Database includes LLaMA 2/3 variants"
    - "Database includes Mistral and Mixtral models"
    - "Database includes Qwen, Phi, DeepSeek, Gemma, Command-R families"
    - "MoE models correctly specify total parameters (not active parameters)"
    - "GQA models correctly specify num_kv_heads"
    - "All model entries pass Zod schema validation"
  artifacts:
    - path: "src/data/models.json"
      provides: "Curated model database with 30+ entries"
      min_lines: 400
    - path: "src/utils/models.test.ts"
      provides: "Validation tests for model data"
      exports: []
  key_links:
    - from: "src/data/models.json"
      to: "src/utils/schemas.ts"
      via: "ModelSchema validation"
      pattern: "validateModels"
---

<objective>
Create curated model database with 30+ popular LLMs sourced from HuggingFace config.json files, including accurate parameter counts, architecture types (dense/MoE), layer configurations, and GQA settings. Database covers LLaMA 2/3, Mistral, Mixtral, Qwen, Phi, DeepSeek, Gemma, and Command-R families.

Purpose: Provide convenient presets for common LLMs while ensuring accuracy for VRAM calculations. Critical to correctly specify total parameters for MoE models and num_kv_heads for GQA architectures.

Output: Validated model database (src/data/models.json) with comprehensive test coverage.
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-data/01-RESEARCH.md
@.planning/phases/01-foundation-data/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Create model database with 30+ validated entries from HuggingFace configs</name>
  <files>
src/data/models.json
  </files>
  <action>
Create src/data/models.json with 30+ popular LLM entries sourced from HuggingFace config.json files. Use the public HuggingFace API to fetch accurate architecture parameters: `https://huggingface.co/{model-id}/raw/main/config.json`

**Required model families (30+ total):**

**LLaMA 2 (3 models):**
1. LLaMA-2-7B - 7B params, dense, hidden_size: 4096, num_hidden_layers: 32, num_attention_heads: 32, num_kv_heads: 32, intermediate_size: 11008
2. LLaMA-2-13B - 13B params, dense, hidden_size: 5120, num_hidden_layers: 40, num_attention_heads: 40, num_kv_heads: 40, intermediate_size: 13824
3. LLaMA-2-70B - 70B params, dense, hidden_size: 8192, num_hidden_layers: 80, num_attention_heads: 64, num_kv_heads: 8 (GQA), intermediate_size: 28672

**LLaMA 3.1 (4 models):**
4. LLaMA-3.1-8B - 8B params, dense, hidden_size: 4096, num_hidden_layers: 32, num_attention_heads: 32, num_kv_heads: 8 (GQA), intermediate_size: 14336
5. LLaMA-3.1-70B - 70B params, dense, hidden_size: 8192, num_hidden_layers: 80, num_attention_heads: 64, num_kv_heads: 8 (GQA), intermediate_size: 28672
6. LLaMA-3.1-405B - 405B params, dense, hidden_size: 16384, num_hidden_layers: 126, num_attention_heads: 128, num_kv_heads: 8 (GQA), intermediate_size: 53248
7. LLaMA-3.2-3B - 3B params, dense, hidden_size: 3072, num_hidden_layers: 28, num_attention_heads: 24, num_kv_heads: 8 (GQA), intermediate_size: 8192

**Mistral (2 models):**
8. Mistral-7B-v0.1 - 7B params, dense, hidden_size: 4096, num_hidden_layers: 32, num_attention_heads: 32, num_kv_heads: 8 (GQA), intermediate_size: 14336
9. Mistral-7B-v0.3 - 7B params, dense, hidden_size: 4096, num_hidden_layers: 32, num_attention_heads: 32, num_kv_heads: 8 (GQA), intermediate_size: 14336

**Mixtral (MoE - 3 models):**
10. Mixtral-8x7B-v0.1 - 46.7B params (TOTAL, not 13B active), architecture: "moe", hidden_size: 4096, num_hidden_layers: 32, num_attention_heads: 32, num_kv_heads: 8, intermediate_size: 14336, num_experts: 8, num_experts_per_token: 2
11. Mixtral-8x22B-v0.1 - 141B params (TOTAL), architecture: "moe", hidden_size: 6144, num_hidden_layers: 56, num_attention_heads: 48, num_kv_heads: 8, intermediate_size: 16384, num_experts: 8, num_experts_per_token: 2
12. Mixtral-8x7B-Instruct-v0.1 - 46.7B params, architecture: "moe", hidden_size: 4096, num_hidden_layers: 32, num_attention_heads: 32, num_kv_heads: 8, intermediate_size: 14336, num_experts: 8, num_experts_per_token: 2

**Qwen (4 models):**
13. Qwen-2.5-7B - 7B params, dense, hidden_size: 3584, num_hidden_layers: 28, num_attention_heads: 28, num_kv_heads: 4 (GQA), intermediate_size: 18944
14. Qwen-2.5-14B - 14B params, dense, hidden_size: 5120, num_hidden_layers: 48, num_attention_heads: 40, num_kv_heads: 8 (GQA), intermediate_size: 13696
15. Qwen-2.5-32B - 32B params, dense, hidden_size: 5120, num_hidden_layers: 64, num_attention_heads: 40, num_kv_heads: 8 (GQA), intermediate_size: 27648
16. Qwen-2.5-72B - 72B params, dense, hidden_size: 8192, num_hidden_layers: 80, num_attention_heads: 64, num_kv_heads: 8 (GQA), intermediate_size: 29568

**Phi (3 models):**
17. Phi-3-mini-4k - 3.8B params, dense, hidden_size: 3072, num_hidden_layers: 32, num_attention_heads: 32, num_kv_heads: 32, intermediate_size: 8192
18. Phi-3-small-8k - 7B params, dense, hidden_size: 4096, num_hidden_layers: 32, num_attention_heads: 32, num_kv_heads: 8 (GQA), intermediate_size: 14336
19. Phi-3-medium-4k - 14B params, dense, hidden_size: 5120, num_hidden_layers: 40, num_attention_heads: 40, num_kv_heads: 10 (GQA), intermediate_size: 17920

**DeepSeek (3 models):**
20. DeepSeek-Coder-V2-Lite - 16B params, architecture: "moe", hidden_size: 2048, num_hidden_layers: 27, num_attention_heads: 16, num_kv_heads: 2, intermediate_size: 10944, num_experts: 64, num_experts_per_token: 6
21. DeepSeek-V2-Lite - 16B params, architecture: "moe", hidden_size: 2048, num_hidden_layers: 27, num_attention_heads: 16, num_kv_heads: 2, intermediate_size: 10944, num_experts: 64, num_experts_per_token: 6
22. DeepSeek-Coder-33B - 33B params, dense, hidden_size: 7168, num_hidden_layers: 62, num_attention_heads: 56, num_kv_heads: 56, intermediate_size: 18432

**Gemma (3 models):**
23. Gemma-2B - 2B params, dense, hidden_size: 2048, num_hidden_layers: 18, num_attention_heads: 8, num_kv_heads: 1 (Multi-Query), intermediate_size: 16384
24. Gemma-7B - 7B params, dense, hidden_size: 3072, num_hidden_layers: 28, num_attention_heads: 16, num_kv_heads: 16, intermediate_size: 24576
25. Gemma-2-9B - 9B params, dense, hidden_size: 3584, num_hidden_layers: 42, num_attention_heads: 16, num_kv_heads: 8 (GQA), intermediate_size: 14336

**Command-R (2 models):**
26. Command-R - 35B params, dense, hidden_size: 8192, num_hidden_layers: 40, num_attention_heads: 64, num_kv_heads: 64, intermediate_size: 22528
27. Command-R-Plus - 104B params, dense, hidden_size: 12288, num_hidden_layers: 64, num_attention_heads: 96, num_kv_heads: 96, intermediate_size: 53248

**Additional popular models (3 models):**
28. Yi-34B - 34B params, dense, hidden_size: 7168, num_hidden_layers: 60, num_attention_heads: 56, num_kv_heads: 8 (GQA), intermediate_size: 20480
29. Falcon-40B - 40B params, dense, hidden_size: 8192, num_hidden_layers: 60, num_attention_heads: 128, num_kv_heads: 128, intermediate_size: 32768
30. MPT-30B - 30B params, dense, hidden_size: 7168, num_hidden_layers: 48, num_attention_heads: 64, num_kv_heads: 64, intermediate_size: 28672

**Format (JSON array):**
```json
[
  {
    "id": "meta-llama-llama-2-7b",
    "name": "LLaMA 2 7B",
    "architecture": "dense",
    "num_parameters_billion": 7,
    "hidden_size": 4096,
    "num_hidden_layers": 32,
    "num_attention_heads": 32,
    "num_kv_heads": 32,
    "intermediate_size": 11008
  },
  {
    "id": "mistralai-mixtral-8x7b-v0.1",
    "name": "Mixtral 8x7B v0.1",
    "architecture": "moe",
    "num_parameters_billion": 46.7,
    "hidden_size": 4096,
    "num_hidden_layers": 32,
    "num_attention_heads": 32,
    "num_kv_heads": 8,
    "intermediate_size": 14336,
    "num_experts": 8,
    "num_experts_per_token": 2
  },
  ...
]
```

**Critical notes:**
1. **MoE models MUST use total parameters** (Mixtral 8x7B = 46.7B, NOT 13B active) - per research pitfall #1
2. **GQA models MUST specify num_kv_heads** (e.g., LLaMA 3.1 has num_kv_heads: 8) - per research pitfall #2
3. Fetch specs from HuggingFace config.json using: `https://huggingface.co/{org}/{model}/raw/main/config.json`
4. For models with multiple versions (Instruct, Chat), use base model config
5. num_parameters_billion should match actual total parameters, not marketing names

**Field mapping from HuggingFace config.json:**
- hidden_size → hidden_size
- num_hidden_layers → num_hidden_layers
- num_attention_heads → num_attention_heads
- num_key_value_heads → num_kv_heads (fallback to num_attention_heads if missing)
- intermediate_size → intermediate_size
- num_local_experts → num_experts (MoE only)
- num_experts_per_tok → num_experts_per_token (MoE only)

Ensure all entries pass ModelSchema validation.
  </action>
  <verify>
npm run typecheck (should pass)
npm run lint (should pass)
Create a temporary test to validate:
```bash
node -e "const {validateModels} = require('./src/utils/schemas.ts'); const models = require('./src/data/models.json'); console.log('Valid:', validateModels(models).length, 'models')"
```
  </verify>
  <done>
- src/data/models.json exists with 30+ model entries
- All entries include required fields: id, name, architecture, num_parameters_billion, hidden_size, num_hidden_layers, num_attention_heads, intermediate_size
- MoE models (Mixtral, DeepSeek) specify total parameters and include num_experts, num_experts_per_token
- GQA models (LLaMA 3, Mistral, Qwen) specify num_kv_heads
- Database includes LLaMA 2/3, Mistral, Mixtral, Qwen, Phi, DeepSeek, Gemma, Command-R families
- All entries pass ModelSchema.parse() validation without errors
  </done>
</task>

<task type="auto">
  <name>Write validation tests for model data</name>
  <files>
src/utils/models.test.ts
  </files>
  <action>
Create comprehensive Vitest tests for model data validation:

**src/utils/models.test.ts:**
```typescript
import { describe, it, expect } from 'vitest'
import { ModelSchema, validateModels } from './schemas'
import modelsData from '@data/models.json'

describe('Model Database Validation', () => {
  it('should have at least 30 models', () => {
    expect(modelsData.length).toBeGreaterThanOrEqual(30)
  })

  it('should validate all model entries against schema', () => {
    const result = validateModels(modelsData)
    expect(result.length).toBe(modelsData.length)
  })

  it('should include LLaMA 2 variants', () => {
    const llama2Models = modelsData.filter((m) => m.name.includes('LLaMA 2'))
    expect(llama2Models.length).toBeGreaterThanOrEqual(3)
  })

  it('should include LLaMA 3 variants', () => {
    const llama3Models = modelsData.filter((m) => m.name.includes('LLaMA 3'))
    expect(llama3Models.length).toBeGreaterThanOrEqual(3)
  })

  it('should include Mistral models', () => {
    const mistralModels = modelsData.filter((m) => m.name.includes('Mistral'))
    expect(mistralModels.length).toBeGreaterThanOrEqual(2)
  })

  it('should include Mixtral MoE models with correct total parameters', () => {
    const mixtral8x7b = modelsData.find((m) => m.id.includes('mixtral-8x7b'))
    expect(mixtral8x7b).toBeDefined()
    expect(mixtral8x7b?.architecture).toBe('moe')
    // Must be 46.7B (total), NOT 13B (active) - research pitfall #1
    expect(mixtral8x7b?.num_parameters_billion).toBeCloseTo(46.7, 1)
    expect(mixtral8x7b?.num_experts).toBe(8)
    expect(mixtral8x7b?.num_experts_per_token).toBe(2)
  })

  it('should include Qwen models', () => {
    const qwenModels = modelsData.filter((m) => m.name.includes('Qwen'))
    expect(qwenModels.length).toBeGreaterThanOrEqual(3)
  })

  it('should include Phi models', () => {
    const phiModels = modelsData.filter((m) => m.name.includes('Phi'))
    expect(phiModels.length).toBeGreaterThanOrEqual(2)
  })

  it('should include DeepSeek models', () => {
    const deepSeekModels = modelsData.filter((m) => m.name.includes('DeepSeek'))
    expect(deepSeekModels.length).toBeGreaterThanOrEqual(2)
  })

  it('should include Gemma models', () => {
    const gemmaModels = modelsData.filter((m) => m.name.includes('Gemma'))
    expect(gemmaModels.length).toBeGreaterThanOrEqual(2)
  })

  it('should include Command-R models', () => {
    const commandRModels = modelsData.filter((m) => m.name.includes('Command-R'))
    expect(commandRModels.length).toBeGreaterThanOrEqual(2)
  })

  it('should have valid parameter counts', () => {
    modelsData.forEach((model) => {
      expect(model.num_parameters_billion).toBeGreaterThan(0)
      expect(model.num_parameters_billion).toBeLessThan(1000) // Sanity check
    })
  })

  it('should have valid architecture configurations', () => {
    modelsData.forEach((model) => {
      expect(model.hidden_size).toBeGreaterThan(0)
      expect(model.num_hidden_layers).toBeGreaterThan(0)
      expect(model.num_attention_heads).toBeGreaterThan(0)
      expect(model.intermediate_size).toBeGreaterThan(0)
    })
  })

  it('should correctly identify MoE vs dense architectures', () => {
    const moeModels = modelsData.filter((m) => m.architecture === 'moe')
    const denseModels = modelsData.filter((m) => m.architecture === 'dense')

    // Should have both types
    expect(moeModels.length).toBeGreaterThan(0)
    expect(denseModels.length).toBeGreaterThan(0)

    // MoE models must have expert fields
    moeModels.forEach((model) => {
      expect(model.num_experts).toBeDefined()
      expect(model.num_experts_per_token).toBeDefined()
    })
  })

  it('should specify num_kv_heads for GQA models', () => {
    const llama3_8b = modelsData.find((m) => m.id.includes('llama-3.1-8b'))
    const mistral7b = modelsData.find((m) => m.id.includes('mistral-7b'))

    // LLaMA 3.1 and Mistral use GQA with num_kv_heads < num_attention_heads
    if (llama3_8b) {
      expect(llama3_8b.num_kv_heads).toBeDefined()
      expect(llama3_8b.num_kv_heads).toBeLessThan(llama3_8b.num_attention_heads)
    }
    if (mistral7b) {
      expect(mistral7b.num_kv_heads).toBeDefined()
      expect(mistral7b.num_kv_heads).toBeLessThan(mistral7b.num_attention_heads)
    }
  })

  it('should validate individual model entry structure', () => {
    const sampleModel = modelsData[0]
    const result = ModelSchema.safeParse(sampleModel)

    expect(result.success).toBe(true)
    if (result.success) {
      expect(result.data).toHaveProperty('id')
      expect(result.data).toHaveProperty('name')
      expect(result.data).toHaveProperty('architecture')
      expect(result.data).toHaveProperty('num_parameters_billion')
    }
  })

  it('should have unique model IDs', () => {
    const ids = modelsData.map((model) => model.id)
    const uniqueIds = new Set(ids)
    expect(uniqueIds.size).toBe(ids.length)
  })

  it('should have consistent hidden_size and intermediate_size ratio', () => {
    // Most models have intermediate_size ≈ 4x hidden_size (FFN expansion)
    const denseModels = modelsData.filter((m) => m.architecture === 'dense')
    denseModels.forEach((model) => {
      const ratio = model.intermediate_size / model.hidden_size
      // Allow some variation (2x to 8x is reasonable)
      expect(ratio).toBeGreaterThan(2)
      expect(ratio).toBeLessThan(8)
    })
  })

  it('should have num_kv_heads <= num_attention_heads', () => {
    modelsData.forEach((model) => {
      if (model.num_kv_heads !== undefined) {
        expect(model.num_kv_heads).toBeLessThanOrEqual(model.num_attention_heads)
      }
    })
  })
})
```

This test suite ensures:
1. Minimum 30 models in database (DATA-06)
2. All entries pass Zod validation
3. Required model families are present
4. MoE models use correct total parameters (not active)
5. GQA models specify num_kv_heads
6. Architecture configs are sensible
  </action>
  <verify>
npm run test (should pass all model validation tests)
npm run test:coverage (should show coverage for model validation)
  </verify>
  <done>
- src/utils/models.test.ts exists with comprehensive test coverage
- All tests pass when running npm run test
- Tests verify minimum 30 models in database
- Tests verify all required model families are present
- Tests verify MoE models use total parameters (e.g., Mixtral 8x7B = 46.7B)
- Tests verify GQA models specify num_kv_heads
- Tests verify all entries pass Zod schema validation
  </done>
</task>

</tasks>

<verification>
1. Run `npm run test` - all model validation tests should pass
2. Verify src/data/models.json contains 30+ entries
3. Verify database includes all required model families (LLaMA, Mistral, Mixtral, Qwen, Phi, DeepSeek, Gemma, Command-R)
4. Spot-check Mixtral 8x7B has 46.7B total params (not 13B)
5. Spot-check LLaMA 3.1 and Mistral models have num_kv_heads specified
6. Run `npm run typecheck` and `npm run lint` - should pass with zero errors
</verification>

<success_criteria>
- Model database includes 30+ popular models with accurate parameter counts, architecture types, and layer configurations
- Database includes LLaMA 2/3, Mistral, Mixtral, Qwen, Phi, DeepSeek, Gemma, Command-R families
- MoE models correctly specify total parameters (Mixtral 8x7B = 46.7B, not 13B active)
- GQA models correctly specify num_kv_heads
- All model entries pass Zod schema validation
- Comprehensive test coverage for data validation
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-data/01-03-SUMMARY.md`
</output>
